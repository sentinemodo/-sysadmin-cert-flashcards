{
  "name": "sysadmin-cert-flashcards",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@playwright/test": "^1.51.1",
    "typescript": "~5.7.3",
    "vite": "^6.2.5",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "dexie": "^4.0.11",
    "zod": "^3.24.2"
  }
}
